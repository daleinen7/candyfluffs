{
  "routes": [
    {
      "method": "GET",
      "path": "/live-events",
      "handler": "live-events.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/live-events/count",
      "handler": "live-events.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/live-events/:id",
      "handler": "live-events.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/live-events",
      "handler": "live-events.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/live-events/:id",
      "handler": "live-events.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/live-events/:id",
      "handler": "live-events.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
